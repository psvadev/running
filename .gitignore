puls.json
.claude/
HANDOFF.md
tests/
# Screenshots. The repo is public and the app is full of real training data, so a debug capture
# dropped in the folder must not be committable by accident — which nearly happened 2026-08-06 when
# a Playwright script wrote its output here via a relative path. Nothing in the app needs a PNG:
# the favicon is an inline SVG data URI and the README carries no images.
*.png
